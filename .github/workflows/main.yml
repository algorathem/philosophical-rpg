name: Push Notification to Discord
on:
  push:
    branches:
      - development

jobs:
  notify:
    runs-on: ubuntu-latest

    steps:
    - name: Discord Notification
      uses: raesene/discord-webhook-action@v1.1.0  
      with:
        webhook_url: ${{ secrets.DISCORD_WEBHOOK_URL }}  
        username: 'GitSync Buddy'  
        avatar_url: 'https://github.githubassets.com/images/modules/logos_page/GitHub-Mark.png' 
        message: |
          🚨 **New Push to Repository: ${{ github.repository }}**
          💻 **Commit by:** ${{ github.actor }}
          📜 **Commit Message:** ${{ github.event.head_commit.message }}
          🌿 **Branch:** ${{ github.ref }}
          🔗 **Commit Link:** [View Commit](${{ github.event.head_commit.url }})
